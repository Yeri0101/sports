<% @bookings.each do |booking| %>
  <%= link_to booking.activity.name, activity_path(booking.activity.id) %>

  <%= link_to booking_path(booking.id), data: {turbo_method: :delete, turbo_confirm: "Delete #{booking.activity.name}?"} do %>
    Delete
  <% end %>
<% end %>
